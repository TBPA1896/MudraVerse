Extract the zip file
open the mvl folder in vs code
create new terminal
type npm install which installs node modules
now run the npm by typing npm run dev
it will give the local host link of the website
